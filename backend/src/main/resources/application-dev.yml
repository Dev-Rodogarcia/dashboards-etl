app:
  environment: dev

server:
  address: 127.0.0.1
  port: 5011

cors:
  origens-permitidas: http://127.0.0.1:5174,http://localhost:5174

security:
  trust-forwarded-headers: false

auth:
  refresh-cookie-secure: false
